---
title: Select
description: Select allows to create a dropdown list of options.
isServerComponent: false
source: /components/ui/select.tsx
themeSource: /components/recipes/select.ts
---

## Installation

```bash
pnpm dreamy add select
```

## Usage

Pass options as an `items` array on `Select.Root`. Each item requires `value` and `label`.

<Wrapper>
	<Select.Root
        width="xs"
        items={[
            { value: "strawberry", label: "Strawberry" },
            { value: "banana", label: "Banana" },
            { value: "orange", label: "Orange" },
        ]}
    >
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
const fruits = [
    { value: "strawberry", label: "Strawberry" },
    { value: "banana", label: "Banana" },
    { value: "orange", label: "Orange" },
];

<Select.Root items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Custom item rendering

Use the `renderItem` prop on `Select.Root` to customise how each option is displayed. The render function receives the full item type from your `items` array, so extra fields such as `icon` are type-safe.

<Wrapper>
    <ItemsSelect />
</Wrapper>

```tsx
const fruits = [
    { value: "cherry", label: "Cherry", icon: <LuCherry /> },
    { value: "banana", label: "Banana", icon: <LuBanana /> },
    { value: "orange", label: "Orange", icon: <LuCitrus /> },
];

<Select.Root
    items={fruits}
    renderItem={(item) => (
        <HStack gap={2}>
            {item.icon}
            <Text>{item.label}</Text>
        </HStack>
    )}
>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Size

Select comes with 4 different sizes.

<Wrapper>
    <Select.Root width="xs" size={"xs"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
    <Select.Root width="xs" size={"sm"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
    <Select.Root width="xs" size={"md"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
    <Select.Root width="xs" size={"lg"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
<Select.Root size="md" items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Trigger Variant

The trigger can be used in `outline` or `solid` style.

<Wrapper>
    <Select.Root width="xs" triggerVariant={"outline"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
    <Select.Root width="xs" triggerVariant={"solid"} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
<Select.Root triggerVariant="outline" items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Variant

The dropdown list can be `plain` (padded, rounded items) or `stretched` (edge-to-edge items).

<Wrapper>
    {["plain", "stretched"].map((variant) => (
        <Select.Root width="xs" variant={variant} items={[
            { value: "strawberry", label: "Strawberry" },
            { value: "banana", label: "Banana" },
            { value: "orange", label: "Orange" },
        ]}>
            <Select.Trigger placeholder="Select a favorite fruit" />
            <Select.Content />
        </Select.Root>
    ))}
</Wrapper>

```tsx
<Select.Root variant="plain" items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Selected Item Background Scheme

You can customize the background color of the selected item. This will only apply if [`selectedStrategy`](#selected-strategy) is set to `both` or `background`.

<Wrapper>
    {["primary", "success", "warning", "info", "error", "none"].map((scheme) => (
        <>
            <Text>{scheme}</Text>
            <Select.Root
                width="xs"
                selectedItemBackgroundScheme={scheme}
                defaultValue="strawberry"
                items={[
                    { value: "strawberry", label: "Strawberry" },
                    { value: "banana", label: "Banana" },
                    { value: "orange", label: "Orange" },
                ]}
            >
                <Select.Trigger placeholder="Select a favorite fruit" />
                <Select.Content />
            </Select.Root>
        </>
    ))}
</Wrapper>

### Controlled

<Wrapper>
    <ControlledSelect />
</Wrapper>

```tsx
export function ControlledSelect() {
    const [value, setValue] = useState<string>("strawberry");

    const fruits = [
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ];

    return (
        <Select.Root
            items={fruits}
            value={value}
            onChangeValue={setValue}
            width="xs"
        >
            <Select.Trigger placeholder="Select a favorite fruit" />
            <Select.Content />
        </Select.Root>
    );
}
```

### Selected Strategy

You can customize how the selected value is marked as selected.

<Wrapper>
    {["both", "checkmark", "background"].map((strategy) => (
        <>
            <Text>{strategy}</Text>
            <Select.Root
                key={strategy}
                selectedStrategy={strategy}
                width="xs"
                items={[
                    { value: "strawberry", label: "Strawberry" },
                    { value: "banana", label: "Banana" },
                    { value: "orange", label: "Orange" },
                ]}
            >
                <Select.Trigger placeholder="Select a favorite fruit" />
                <Select.Content />
            </Select.Root>
        </>
    ))}
</Wrapper>

### With one icon

Single icon is useful if you want to have a global icon for the trigger, instead of having it on each item.

<Wrapper>
    <Select.Root width="xs" items={[
        { value: "cherry", label: "Cherry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" icon={<LuCherry />} />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
<Select.Root items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" icon={<LuCherry />} />
    <Select.Content />
</Select.Root>
```

### Clearable

Pass `isClearable` prop to enable clear button.

<Wrapper>
    <Select.Root width="xs" isClearable items={[
        { value: "cherry", label: "Cherry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

### Multiple

You can use the `isMultiple` prop to allow multiple selections. `onChangeValue` returns an array of values. By default, the trigger shows selected labels joined with commas (truncated when they overflow).

<Wrapper>
    <Select.Root width="xs" isMultiple items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
<Select.Root isMultiple items={fruits}>
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
// Trigger shows: "Strawberry, Banana" (truncated with ellipsis when too long)
```

### Custom Multiple Selected Text

Use `multipleSelectedText` on `Select.Trigger` to override the default comma-separated labels.

<Wrapper>
    <Select.Root width="xs" isMultiple items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger
            placeholder="Select a favorite fruit"
            multipleSelectedText={(selected) => `${selected.length} selected`}
        />
        <Select.Content />
    </Select.Root>
</Wrapper>

```tsx
<Select.Trigger
    placeholder="Select a favorite fruit"
    multipleSelectedText={(selected) => `${selected.length} selected`}
/>
```

### Async loading

You can fetch data when the Select is opened and update the `items` array. Use `showItems={false}` on `Select.Content` to render custom UI (such as a loading spinner) instead of the default items list.

<Wrapper>
    <AsyncSelect />
</Wrapper>

```tsx
export function AsyncSelect() {
    const [isLoading, setIsLoading] = useState(false);
    const [items, setItems] = useState<{ value: string; label: string }[]>([]);

    function fetchFruits() {
        if (items.length > 0) return;

        setIsLoading(true);
        fetch("/api/fake-select-data")
            .then((res) => res.json())
            .then((data: string[]) =>
                setItems(data.map((fruit) => ({ value: fruit, label: fruit })))
            )
            .finally(() => setIsLoading(false));
    }

    return (
        <Select.Root items={items} onOpen={fetchFruits} width="xs">
            <Select.Trigger placeholder="Select a favorite fruit" />
            <Select.Content showItems={!isLoading}>
                <Spinner color="primary" py={4} />
            </Select.Content>
        </Select.Root>
    );
}
```

### Virtualized

For better performance with large lists (100+ items), use `Select.VirtualContent` instead of `Select.Content`. It only renders visible items.

<Wrapper>
    <Select.Root
        width="xs"
        items={Array.from({ length: 250 }, (_, index) => ({
            value: index.toString(),
            label: `Item ${index + 1}`,
        }))}
    >
        <Select.Trigger placeholder="Select a number" />
        <Select.VirtualContent />
    </Select.Root>
</Wrapper>

```tsx
const manyItems = Array.from({ length: 250 }, (_, index) => ({
    value: index.toString(),
    label: `Item ${index + 1}`,
}));

<Select.Root items={manyItems}>
    <Select.Trigger placeholder="Select a number" />
    <Select.VirtualContent />
</Select.Root>
```

#### Props

- `estimatedItemHeight` - Estimated height of each item in pixels. For different select sizes: `xs`: `26`, `sm`: `28`, `md`: `32`, `lg`: `40`. Default: `32`
- `overscan` - Number of items to render outside the visible area. Default: `5`
- `maxHeight` - Maximum height of the virtualized list container in pixels. Default: `300`

### Inside modals and popovers

By default, the select dropdown uses the `dropdown` z-index layer. When rendered inside a modal or popover, `Select.Root` automatically raises the z-index. You can also set it explicitly with `layer` or `contentZIndex`.

<Wrapper>
    <SelectInModal />
</Wrapper>

```tsx
<Select.Root items={fruits} layer="popover">
    <Select.Trigger placeholder="Select a favorite fruit" />
    <Select.Content />
</Select.Root>
```

### Close on select

You can customize whether the Select should close when an item is selected. Default is `true` for non-multiple select, `false` for multiple select.

<Wrapper>
    <Select.Root width="xs" closeOnSelect={false} items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>

### Reduce motion

You can disable the animation of the Select by setting the `reduceMotion` prop to `true`.

<Wrapper>
    <Select.Root width="xs" reduceMotion items={[
        { value: "strawberry", label: "Strawberry" },
        { value: "banana", label: "Banana" },
        { value: "orange", label: "Orange" },
    ]}>
        <Select.Trigger placeholder="Select a favorite fruit" />
        <Select.Content />
    </Select.Root>
</Wrapper>
