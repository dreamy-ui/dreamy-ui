---
title: Switch
description: Switch is used to select one or more options from a list.
isServerComponent: false
source: /packages/react/src/components/switch/switch.tsx
themeSource: /packages/system/src/recipes/switch.ts
---

## Import

- `Switch` - A Switch component.

```jsx
import { Switch } from "@dreamy-ui/react";
```

## Usage

<Wrapper>
	<Switch>
		<>Default</>
	</Switch>
</Wrapper>

```tsx
<Switch>
	Default
</Switch>
```

### Scheme

Change the color scheme of the Switch.

<Wrapper>
	{["primary", "secondary", "success", "warning", "error", "info"].map((scheme) => (
		<Switch key={scheme} scheme={scheme} defaultChecked>
			{scheme}
		</Switch>
	))}
</Wrapper>

```tsx
{["primary", "secondary", "success", "warning", "error", "info"].map((scheme) => (
	<Switch key={scheme} scheme={scheme}>
		{scheme}
	</Switch>
))}
```

### Size

Change the Switch size.

<Wrapper>
	{["sm", "md", "lg"].map((size) => (
		<Switch key={size} size={size}>
			{size}
		</Switch>
	))}
</Wrapper>

```tsx
{["sm", "md", "lg"].map((size) => (
	<Switch key={size} size={size}>
		{size}
	</Switch>
))}
```

### Controlled

Use `isChecked` and `onChange`, `onChangeValue` to control the checkbox.

<Wrapper>
	<ControlledSwitch />
</Wrapper>

```tsx
export function ControlledSwitch() {
    const [isChecked, setIsChecked] = useState(false);

    return (
        <>
            <Text>Selected: {isChecked ? "true" : "false"}</Text>
            <Switch
                isChecked={isChecked}
                onChangeValue={setIsChecked}
            >
                Controlled
            </Switch>
        </>
    );
}
```